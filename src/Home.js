import React from "react";
import { Outlet } from "react-router-dom";

const Home = (props) => {
  return (
    /*<section> {props.title} </section>*/
    <div>
      <h1>Home Page!</h1>
      {/*<Outlet />*/}
    </div>
  );
};

export default Home;
